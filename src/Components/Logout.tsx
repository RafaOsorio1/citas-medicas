import React from 'react';
import {auth} from '../firebase/firebase';



export const Logout = () => {
  return (
    <div>
        <button>
            Cerrar sesión
        </button>
    </div>
  )
}
